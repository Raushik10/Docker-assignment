# Flask + Node.js Form Submission (Docker Compose)

## Project Structure
- **Frontend:** Node.js + Express (port 3000)
- **Backend:** Flask (port 5000)
- Saves submitted form data into **data.txt** (no MongoDB)

## Run using Docker Compose
```bash
docker-compose up --build
